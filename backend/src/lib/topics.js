// The topics a post can be submitted to. Mirrors DYNAMIC_TOPICS in
// app/src/routes.jsx - the two run in separate deployables, so this is a
// deliberate, small duplication rather than a shared package. Keep in sync.
export const TOPIC_SLUGS = ["gs", "qg", "ml", "math", "drones", "ip", "mb", "bioinfo", "history", "archive"];
